using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Data.Repository;
using System.Linq;

namespace Application.UnitTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethodJson()
        {
            //var probabilities = new RepositoryProbability().GetListAll().ToList();
            //var text = MapJsonProbability.MapJson(probabilities.Find(i=>i.Id==1));
            //var text1 = MapJsonProbability.MapJsonList(probabilities);
            //var text2 = MapJsonProbability.MapJsonText(probabilities.Find(i => i.Id == 1));
            //Console.WriteLine(text2);
        }
    }
}
